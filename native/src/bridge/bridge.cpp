#include "autoplay/bridge.hpp"

namespace autoplay::native {
// Transport implementations (named pipe/shared memory/socket) belong here later.
// Keeping the protocol independent prevents the AI layer from depending on Windows APIs.
}
